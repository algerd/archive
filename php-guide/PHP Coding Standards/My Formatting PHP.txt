             Правила форматирования кода PHP.

Список инструкций:
- управляющая конструкция (if, foreach и т.д.) : сокр - упрконстр
- функции
- классы
- методы
    			 
	
1. Открывающая фигурная скобка { пишется на той же строке, что и упрконстр с отступом 1 пробел:
    function getName () {
    }
    class User {
    }

2. Стремиться, чтобы длина строки не превышала 100 символов.

3. Весь код внутри инструкции пишется с отступом табуляция = 4 пробела:
    if ( $a > $b ) {
        $str = 'Hello';

4. В управл конструкциях, объявлении функций или методов после открывающей и перед закрывающей круглой скобкой делать отступ 1 пробел,
   если содержимое скобок плохочитаемо или имеет большой размер :
    switch ($name) - можно не делать отступ
    function getName($name) - можно не делать
    for ( $i = 0; $g < 10; $i++ ) { - здесь надо поставить,чтобы код внутри скобок был читабельнее
	
5. Все операторы =, +, & и др. отделяются с обоих сторон 1 пробелом:
	if ( $a == $b ){

6. Круглая скобка в управл конструкциях, кроме функций и методов, пишется с лывым отступом = 1 пробел:
    foreach ( $a as $b ){
	
5. Круглая скобка в функции, методе (при объявлении или вызове) пишется без отступа:
	function setNameCorp( $rgum, $count ) { 
        setName($a) * $b
        new Class()
        $obj->getName()

6. Аргументы или выражения в круглых скобках инструкций могут писаться с крайним отступом 1 пробел, если аргументов несколько или они трудночитаемые.
   Перечисление аргументов - запятая без левого отступа и с правым отступом 1 пробел:
	function setName( $a, $b + $c, $h ) {
	
7. Блоки кода надо визуально разделять пустой строкой приблизительно 1 отступ на 8-10 строк. Закрывающие фигурная или круглая скобка на отдельной строке можно рассматривать как пустую.


8. В переносах строк (сознательном дляподчёркивания логики или вынужденном из-зи длины строки) соблюдать стандартный отступ - 1табуляция:
    SetFontAttributes( faceName[ fontId ], size[ fontId ], bold[ fontId ],
        italic[ fontId ], syntheticAttribute[ fontId ].underline,
        syntheticAttribute[ fontId ].strikeout );
    if ( $name < $kklj or $kjklj > jjkh ) and
        ( $jhjh == hkjh or $ggjg > jhj ) {

	
9. Не выравнивать строки по знаку, скобкам или другим причинам:
    $jjhkjh = 'xfcbdfbd';
    $gbj    = 'hkhkhlk'; - неправильно!
    Надо:
    $jjhkjh = 'xfcbdfbd';
    $gbj = 'hkhkhlk'; - правильно!
	
10. Методы класса и функции пишутся с отступом в 1 строку.:
        public function setName () {}

	// Коммент  cncnvmngfmgfm
	public function setName () {	
	}
	
11. Комментарии, относящиеся к строке - писать на той же строке что и код. Избегать такие комменты.
    $str = 'Hello';     // Привет

12. Комментарии, относящиеся к блоку писать над блоком с отступом первой строки блока.
    И над комментом оставлять 1 пустую строку.

13. Упорядочивайте методы по алфавиту
	
